import { FillDirective } from './fill.directive';

describe('FillDirective', () => {
  it('should create an instance', () => {
    const directive = new FillDirective();
    expect(directive).toBeTruthy();
  });
});
